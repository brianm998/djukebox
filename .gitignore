Packages
.build
xcuserdata
*.xcworkspace
# but do track the intentional top-level multi-project workspace
# (its user-specific state is still ignored by the xcuserdata rule above)
!djukebox.xcworkspace
Pods/
**/*~
**/#*
**/.#*
DerivedData/
.DS_Store
db.sqlite*
.swiftpm

